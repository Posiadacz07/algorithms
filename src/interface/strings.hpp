#ifndef STRINGS_HPP
#define STRINGS_HPP

#include <string>
#include <vector>

namespace strings {

std::vector<int> searchPatternBruteForce(std::string source,
                                         std::string pattern);

} // namespace strings

#endif // STRINGS_HPP